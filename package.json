{
  "name": "gemini-plugin-cc",
  "version": "0.5.0",
  "description": "Claude Code plugin to call Google Gemini for ask, review, and long-form research. Tests use Node's built-in test runner — no external dev dependencies.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "homepage": "https://github.com/taibaran/gemini-plugin-cc",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taibaran/gemini-plugin-cc.git"
  }
}
